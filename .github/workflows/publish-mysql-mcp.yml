name: Publish local-mysql-mcp-server

on:
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/publish-mcp.yml
    secrets: inherit
    with:
      package_path: mcps/MySQL
      package_name: local-mysql-mcp-server
