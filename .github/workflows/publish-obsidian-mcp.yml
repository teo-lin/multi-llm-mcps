name: Publish @teolin/mcp-obsidian

on:
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/publish-mcp.yml
    secrets: inherit
    with:
      package_path: mcps/Obsidian
      package_name: "@teolin/mcp-obsidian"
