name: Publish atlassian-mcp-server

on:
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/publish-mcp.yml
    secrets: inherit
    with:
      package_path: mcps/Atlassian
      package_name: atlassian-mcp-server
