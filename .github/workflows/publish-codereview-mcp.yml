name: Publish @teolin/code-review-agent

on:
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/publish-mcp.yml
    secrets: inherit
    with:
      package_path: mcps/CodeReview
      package_name: "@teolin/code-review-agent"
