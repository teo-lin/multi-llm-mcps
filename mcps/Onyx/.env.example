# Base URL of your self-hosted Onyx instance (nginx), WITHOUT the /api suffix
ONYX_BASE_URL=http://localhost:3210

# Onyx login (API keys require the Business plan, so the MCP logs in like the UI)
ONYX_EMAIL=you@example.com
ONYX_PASSWORD=your-onyx-password
