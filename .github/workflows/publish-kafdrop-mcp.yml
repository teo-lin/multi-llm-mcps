# DISABLED: Kafdrop MCP is not working. Remove "private": true in mcps/Kafdrop/package.json to re-enable.
name: "[DISABLED] Publish @teolin/mcp-kafdrop"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Kafdrop MCP publishing is disabled — the server does not work yet."
          exit 1
